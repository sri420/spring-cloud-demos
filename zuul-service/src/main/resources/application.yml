eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
server:
  port: 54321
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    env:
      enabled: true

