spring:
  application:
    name: eureka-service
eureka:
 client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:12345/eureka
server:
  port: 12345
management:
  endpoints:
    web:
      exposure:
        include: "*"
